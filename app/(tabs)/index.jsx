import { Hospital } from 'lucide-react-native'
import { SafeAreaView } from 'react-native-safe-area-context'

const index = () => {
  return (
    <SafeAreaView>
      <Hospital size={48} color="black" />
    </SafeAreaView>
  )
}

export default index